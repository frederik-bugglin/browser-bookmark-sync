# TODO

## Post-MVP / Polish

- [x] ~~**Sync-Status `warning` bei offenen Konflikten.**~~ Erledigt 2026-05-26.
- [ ] **Safari iCloud-Quirk dokumentieren.** Bei aktivem iCloud Safari-Sync persistiert Safari neu hinzugefügte Bookmarks nicht sofort in `~/Library/Safari/Bookmarks.plist` — Junction's Read sieht sie dann nicht. Erst durch eine Folge-Aktion (Move, Edit, längere Wartezeit) schreibt Safari die plist. Mögliche Fixes: (a) Doku-Hinweis im Safari-Onboarding ("kurz warten oder Bookmark verschieben"), (b) zusätzliches Lesen aus iCloud-CloudBookmarks-Bereich (komplex).
- [x] ~~**Chrome `dateModified` fehlt im Konflikt-Detail.**~~ Erledigt 2026-05-26. Root cause: Chromium speichert `date_modified` nur auf Folders, nicht auf URL-Bookmarks (von 9217 URLs im echten Profil hatten 0 ein `date_modified`). Lösung: Sync-Engine stempelt im `diff`-Schritt fehlende `dateModified` mit der Sync-Run-Zeit, wenn ein Change detected wurde. Bonus-Fix: `write.ts` konvertierte ISO-Strings nicht zurück in Chromium-Time-Format — neue `isoToChromiumTime()` ergänzt, plus Round-Trip-Regression-Test.
- [ ] **Firefox-Adapter: Read bei laufendem Browser ermöglichen.** Die Walkthrough-Doku (`docs/qa/end-to-end-sync-walkthrough.md`, Test 6) verspricht, dass Firefox-Bookmarks bei offenem Browser zumindest gelesen werden (Adapter kopiert `places.sqlite` vor dem Read). Aktuell überspringt `electron/sync-engine/drivers.ts:140-146` Firefox aber komplett (Read + Write) wenn der Profile-Lock gehalten wird. Entweder Code so anpassen, dass nur Write blockiert wird, oder Doku entsprechend korrigieren.
- [ ] **Bookmarks landen nach Sync im falschen Root-Ordner.** Beim Walkthrough-Cleanup zeigten sich die Test-Bookmarks in Firefox unter "Weitere Lesezeichen" (`unfiled`), obwohl sie ursprünglich in "Lesezeichen-Symbolleiste" (`toolbar`) angelegt wurden. Vermutete Ursache: Conflict-Restore in Test 5 oder der Cleanup-Delete-Sync hat den `rootKey` nicht 1:1 propagiert. Sync-Engine prüfen: bleibt der Root-Ordner beim Restore und beim Cross-Browser-Write erhalten?
